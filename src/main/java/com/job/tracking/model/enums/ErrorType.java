package com.job.tracking.model.enums;

public enum ErrorType {
    VALIDATION_ERROR_TYPE,
    PROCESSING_ERROR_TYPE,
    DATABASE_ERROR_TYPE,
    FATAL_ERROR_TYPE,
    TASK_NOT_FOUND,
    TASK_DOES_NOT_EXIST,
    USER_NOT_FOUND
}
